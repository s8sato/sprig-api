build:
  docker:
    web:
      dockerfile: Dockerfile
      target: prod
    releaser:
      dockerfile: Dockerfile
      target: migration
release:
  image: releaser
  command:
    - diesel migration run
