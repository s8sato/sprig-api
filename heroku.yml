# setup:
#   addons:
#   - plan: heroku-postgresql:hobby-dev
#     as: DATABASE
build:
  docker:
    web:
      dockerfile: Dockerfile
      target: prod
    worker:
      dockerfile: Dockerfile
      target: build
release:
  image: worker
  command:
    - diesel migration run
