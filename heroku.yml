setup:
  addons:
  - plan: heroku-postgresql:hobby-dev
    as: DATABASE
build:
  docker:
    demo:
      dockerfile: Dockerfile
      target: prod
release:
  image: demo
  command:
    - ./release.sh
